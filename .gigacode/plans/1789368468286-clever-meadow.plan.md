# Plan: Extract EditPrinterModal to standalone component

## Goal
Extract the `EditPrinterModal` component from `PrintersPage.tsx` into a new standalone file `frontend/src/components/EditPrinterModal.tsx` and connect it properly.

## Current State

### What exists
- **Inline component** at `PrintersPage.tsx` lines 7865–8127 (`function EditPrinterModal`)
- **State variable** at line 2172: `const [showEditModal, setShowEditModal] = useState(false);`
- **Render trigger** at lines 7279–7284: `{showEditModal && (<EditPrinterModal .../>)}`
- **Open handler** at lines 3745–3757: menu button sets `setShowEditModal(true)`
- **Test file**: `frontend/src/__tests__/components/EditPrinterPreflight.test.tsx`

### Dependencies used by EditPrinterModal
| Import | Source | Already in PrintersPage? |
|--------|--------|--------------------------|
| `useTranslation` | `react-i18next` | Yes |
| `useQueryClient` | `@tanstack/react-query` | Yes |
| `useMutation` | `@tanstack/react-query` | Yes |
| `useToast` | `../contexts/ToastContext` | Yes |
| `Printer` | `../api/client` | Yes |
| `PrinterCreate` | `../api/client` | Yes |
| `PrinterDiagnosticResult` | `../api/client` | Yes |
| `Card`, `CardContent` | `../components/Card` | Yes |
| `Button` | `../components/Button` | Yes |
| `AlertTriangle` | `lucide-react` | Yes |
| `Wrench` | `lucide-react` | Yes |
| `DiagnosticChecklist` | `../components/ConnectionDiagnostic` | Yes |

## Implementation Steps

### Step 1: Create `EditPrinterModal.tsx`
**File**: `frontend/src/components/EditPrinterModal.tsx`

1. Create new file with all imports from the list above
2. Copy the full `EditPrinterModal` function (lines 7865–8127)
3. Export it as default: `export default EditPrinterModal`

### Step 2: Update `PrintersPage.tsx`

1. **Add import** at the top of the file (with other modal imports):
   ```typescript
   import { EditPrinterModal } from '../components/EditPrinterModal';
   ```

2. **Remove** the inline `EditPrinterModal` function definition (lines 7865–8127, ~263 lines)

   **Note:** `AlertTriangle` and `Wrench` are used elsewhere in PrintersPage (HMS badge, delete dialog, bed jog warning, maintenance mode) — do NOT remove them.

5. **Keep** the render trigger (lines 7279–7284) — it stays in PrintersPage

6. **Keep** the open handler (lines 3745–3757) — it stays in PrintersPage

### Step 3: Verify test compatibility
The test file `EditPrinterPreflight.test.tsx` expects the modal to render with text "Edit Printer" — this will still work since the component is the same, just in a different file.

## Files to modify
| File | Action |
|------|--------|
| `frontend/src/components/EditPrinterModal.tsx` | **CREATE** — new standalone component |
| `frontend/src/pages/PrintersPage.tsx` | **EDIT** — add import, remove inline function (~263 lines), remove unused imports |

## Verification
1. Run TypeScript check: `npx tsc --noEmit` in `frontend/`
2. Run tests: `npm test -- EditPrinterPreflight`
3. Manual check: open PrintersPage, click edit on a printer, verify modal works identically
